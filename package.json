{
  "name": "my-webhook-proxy",
  "version": "1.0.0",
  "description": "A Vercel serverless function to process and forward trading signals.",
  "main": "api/webhook-proxy.js",
  "dependencies": {
    "node-fetch": "^2.7.0",
    "abort-controller": "^3.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
